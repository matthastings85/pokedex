import React, { useState } from "react";

// Components
import Search from "./Search";
import Card from "./Card";

// Utilities
import { POKEURL, useFetch } from "../utilities";

const Pokedex = () => {
  const [loadedPokemon, setLoadedPokemon] = useState([]);

  return;
};

export default Pokedex;
